

<div class="container mt-5">
  <h2>Vos réservations:</h2>

  <div class="row">
  <% @bookings.each do |booking| %>
    <div class="col-12 col-md-6 col-lg-4 mb-4 d-flex align-items-stretch">
      <div class="experiences_card d-flex flex-column h-100 w-100">
        <%= link_to booking_path(booking), class: "text-decoration-none text-dark flex-grow-1 d-flex flex-column" do %>
          <% if booking.experience.photo.attached? %>
            <div class="text-center">
              <%= image_tag booking.experience.photo, alt: "Image de l'expérience", class: "img-fluid rounded shadow-lg experiences-img flex-shrink-0" %>
            </div>
          <% else %>
            <p class="text-muted text-center">Aucune photo disponible pour cette expérience.</p>
          <% end %>

          <h4 class="mt-3 flex-grow-1"><%= booking.experience.name %></h4>
          <p class="mt-1 ms-2 text-muted fst-italic text-center flex-shrink-0">🎭 <%= booking.experience.stage_name %></p>

          <div class="experiences_body flex-grow-1 d-flex flex-column justify-content-between">
            <p><i class="fa-solid fa-music"></i> <%= booking.experience.musical_genre %></p>
            <p><i class="fa-solid fa-star"></i> <%= booking.experience.absurd_requirement %></p>
            <p class="price flex-shrink-0"><i class="fa-solid fa-euro-sign"></i> <%= booking.experience.price_per_day%>€/jour</p>
            <p><i class="fa-solid fa-calendar-days"></i> Du: <%= booking.date_start %> Au: <%= booking.date_end %></p>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
</div>

</ul>
